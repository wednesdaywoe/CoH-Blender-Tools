# Python build artifacts (regenerated by the interpreter; must never be tracked
# or shipped in the addon zip — build_zip.sh strips them too).
__pycache__/
*.pyc
*.pyo

# Editor / OS cruft
*.swp
.DS_Store
